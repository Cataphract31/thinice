# Baked into the bundle at build time (Vite). This is what turns the deploy
# into the REAL networked game instead of the offline demo — see DEPLOYMENT.md
# in the main repo. Not a secret: it is just the public beta server address.
VITE_SERVER_URL=wss://35.184.125.131.sslip.io
