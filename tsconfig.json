{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    /* Dead code is a correctness signal, not just clutter: every one of the
       six the strip left behind was a demo-era constant that no longer meant
       anything, and nothing flagged them. Now the typecheck does. */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "types": ["node"],
    "paths": {
      "@zinc/engine": ["./packages/engine/src/index.ts"]
    }
  },
  // Engine only. The web app is browser code with its own DOM lib and
  // JSX settings, and is checked by apps/web/tsconfig.json — checking it here
  // as well just reports every DOM global as missing.
  "include": ["packages/**/*.ts"]
}
